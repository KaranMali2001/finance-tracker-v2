#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Change to the repo root directory (where git is initialized)
cd "$(dirname -- "$0")/../.." || exit

# Run lint-staged from finance-tracker-web directory
cd finance-tracker-web || exit
npx lint-staged

